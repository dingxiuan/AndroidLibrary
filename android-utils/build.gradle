apply plugin: 'com.android.library'

apply from: "${rootDir}/buildSystem/AndroidConfig.gradle"

android {
    defaultConfig {
        versionCode 1
        versionName "1.0"
    }
}

dependencies {
    implementation fileTree(dir: 'libs', include: ['*.jar'])
}
